import { Button, Text } from '@rolder/ui-kit-react';
import './App.css';

const App = () => {
  return (
    <div>
      <Text muted p={10}>
        TEST
      </Text>
      <Button muted size="lg">
        BUTTON
      </Button>
    </div>
  );
};

export default App;
