id: "ds/ss-react"

project:
  name: "Styled system for React"
  description: "Styled system for React"

tasks:
  build:
    command: "panda && bun panda codegen"
    outputs:
      - "./*"
  dev:
    command: "bun panda codegen -w"
  publish:
    command: "bun publish"
    local: true
